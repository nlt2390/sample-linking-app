import React from 'react';
import {
  PostDetailsContainer
} from '../../containers';

const PostDetailsScreen = (props) => {
  return <PostDetailsContainer route={props.route} />;
};

export default PostDetailsScreen;
