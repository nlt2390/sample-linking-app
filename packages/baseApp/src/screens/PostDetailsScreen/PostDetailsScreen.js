import React from 'react';
import { PostDetailsScreenPackage } from 'package-app';

const PostDetailsScreen = (props) => {
  return <PostDetailsScreenPackage route={props.route} />;
};

export default PostDetailsScreen;
